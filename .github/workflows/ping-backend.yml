# name: Keep Render Backend Awake

# on:
#   schedule:
#     # Every 10 minutes
#     - cron: "*/10 * * * *"
#   workflow_dispatch:

# jobs:
#   ping:
#     runs-on: ubuntu-latest
#     steps:
#       - name: ping backend
#         run: |
#           echo "Pinging backend..."
#           curl https://seattlehourly.onrender.com/api/fetch/ping
